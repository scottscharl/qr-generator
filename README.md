# QR Code Generator

This is a simple HTML page that allows the user to enter a string of text and convert it to a QR code that can be downloaded or shared. The generation happens client-side.

The package used is [`qrcode-generator`](https://www.npmjs.com/package/qrcode-generator).

Feel free to deploy this to a domain you own!
